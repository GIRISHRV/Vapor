name: Vapor CI/CD

on:
  push:
    branches: [ "main", "master" ]
    tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    name: Build & Release
    runs-on: windows-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          
      - name: Install dependencies
        working-directory: apps/app
        run: flutter pub get
        
      - name: Run Linter
        working-directory: apps/app
        run: flutter analyze

      - name: Run Tests
        working-directory: apps/app
        run: flutter test
        
      - name: Build Web
        working-directory: apps/app
        run: flutter build web --release
        
      - name: Build Android APK
        working-directory: apps/app
        run: flutter build apk --release
        
      - name: Build Windows
        working-directory: apps/app
        run: flutter build windows --release
        
      - name: Zip Windows Build
        working-directory: apps/app/build/windows/x64/runner/Release
        run: Compress-Archive -Path * -DestinationPath ../../../../../vapor-windows.zip
        
      - name: Zip Web Build
        working-directory: apps/app/build/web
        run: Compress-Archive -Path * -DestinationPath ../../../vapor-web.zip
        
      - name: Generate Changelog
        if: startsWith(github.ref, 'refs/tags/')
        id: changelog
        run: |
          git log $(git describe --tags --abbrev=0 HEAD^)..HEAD --pretty=format:"- %s" > changelog.txt
          echo "notes=$(cat changelog.txt)" >> $env:GITHUB_OUTPUT
        continue-on-error: true
        
      - name: Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            apps/app/build/app/outputs/flutter-apk/app-release.apk
            vapor-windows.zip
            vapor-web.zip
          body_path: changelog.txt
          generate_release_notes: true

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_VAPOR_ENGINE }}'
          projectId: vapor-engine
          channelId: live
          entryPoint: apps/app
